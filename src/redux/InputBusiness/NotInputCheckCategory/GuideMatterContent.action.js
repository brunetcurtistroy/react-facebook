import { message } from "antd";
import GuideMatterContentService from "services/InputBusiness/NotInputCheckCategory/GuideMatterContentService";

const GuideMatterContentAction = {
    getScreenData(data) {
        return GuideMatterContentService.GetScreenData(data)
            .then((res) => {
                return res?.data;
            })
            .catch((err) => {
                const res = err.response;
                if (!res || !res.data || !res.data.message) {
                    message.error("エラーが発生しました");
                    return;
                }
                message.error(res.data.message);
            });
    },
    Change_AutomaticJudgeBasicJudge(data) {
        return GuideMatterContentService.Change_AutomaticJudgeBasicJudge(data)
            .then((res) => {
                return res?.data;
            })
            .catch((err) => {
                const res = err.response;
                if (!res || !res.data || !res.data.message) {
                    message.error("エラーが発生しました");
                    return;
                }
                message.error(res.data.message);
            });
    },
    Change_Hide(data) {
        return GuideMatterContentService.Change_Hide(data)
            .then((res) => {
                return res?.data;
            })
            .catch((err) => {
                const res = err.response;
                if (!res || !res.data || !res.data.message) {
                    message.error("エラーが発生しました");
                    return;
                }
                message.error(res.data.message);
            });
    },
    Exit(data) {
        return GuideMatterContentService.Exit(data)
            .then((res) => {
                return res?.data;
            })
            .catch((err) => {
                const res = err.response;
                if (!res || !res.data || !res.data.message) {
                    message.error("エラーが発生しました");
                    return;
                }
                message.error(res.data.message);
            });
    },
    Change_Lio_LeadershipMatters(data) {
        return GuideMatterContentService.Change_Lio_LeadershipMatters(data).then((res) => {
            return res?.data;
        })
            .catch((err) => {
                const res = err.response;
                if (!res || !res.data || !res.data.message) {
                    message.error("エラーが発生しました");
                    return;
                }
                message.error(res.data.message);
            });
    },

}

export default GuideMatterContentAction;